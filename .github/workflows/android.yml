name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch: # Allows manual trigger from GitHub website on phone!

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: |
        if [ -f gradlew ]; then
          chmod +x gradlew
        fi

    - name: Build Debug APK
      run: |
        if [ -f gradlew ]; then
          ./gradlew assembleDebug -Pkotlin.compiler.execution.strategy=daemon
        else
          gradle assembleDebug -Pkotlin.compiler.execution.strategy=daemon
        fi

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Ki-Lagbe-Rider-APK
        path: app/build/outputs/apk/debug/app-debug.apk
